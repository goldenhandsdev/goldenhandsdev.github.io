sap.ui.define([
    "sap/ui/core/mvc/Controller"
], function (Controller) {
    "use strict";

    return Controller.extend("projectlist.controller.App", {
        
        /**
         * Called when the controller is instantiated.
         * @public
         */
        onInit: function () {
            // Initialize the app controller
            // The router will handle the navigation to the master view
        }
    });
});
